<UserControl x:Class="ResizeAndConvertImages.FolderSelector"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="400" d:DesignWidth="600">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Root Folder Selection -->
        <StackPanel Grid.Row="0" Orientation="Vertical">
            <Button x:Name="BrowseButton" 
                    Style="{StaticResource ButtonTextStyle}"
                    Content="Select Folders" Click="BrowseButton_Click" HorizontalAlignment="Center"/>
            <TextBox x:Name="RootFolderTextBox" Width="Auto"  Height="30" IsReadOnly="true"  
                     VerticalContentAlignment="Center" TextWrapping="Wrap" VerticalAlignment="Top" />
        </StackPanel>

        <TreeView x:Name="FolderTreeView"  Grid.Row="1"  MaxHeight="600">
            <TreeView.ItemTemplate>
                <HierarchicalDataTemplate ItemsSource="{Binding SubFolders}">
                    <StackPanel Orientation="Horizontal">
                        <CheckBox IsChecked="{Binding IsSelected, Mode=TwoWay}" />
                        <TextBlock Text="{Binding Name}" Margin="5,0" />
                    </StackPanel>
                </HierarchicalDataTemplate>
            </TreeView.ItemTemplate>
        </TreeView>

        <!-- Process Button -->
        <StackPanel Grid.Row="2" Orientation="Horizontal">
         <Button x:Name="CheckAll" 
                Width="100"
                Style="{StaticResource ButtonTextStyle}"
                Content="Check All"  
                IsEnabled="{Binding Enabled, RelativeSource={RelativeSource AncestorType=UserControl}}" Click="CheckAll_Click"/>
        <Button x:Name="ClearChecks" 
                Width="100"
                Style="{StaticResource ButtonTextStyle}"
                Grid.Row="2" Content="Clear Checks" 
                IsEnabled="{Binding Enabled, RelativeSource={RelativeSource AncestorType=UserControl}}" Click="ClearChecks_Click"/>
        </StackPanel>
        <Button x:Name="ProcessButton" 
                Style="{StaticResource ButtonTextStyle}"
                Grid.Row="2" Content="Process Selections" HorizontalAlignment="Right" 
                Click="ProcessButton_Click" IsEnabled="{Binding Enabled, RelativeSource={RelativeSource AncestorType=UserControl}}"/>

    </Grid>
</UserControl>